import React from 'react'
import "./styles.scss"

const CustomInput = ({placeholder}) => {
  return (
    <input type="text" className='cart_form_input' placeholder={placeholder}/>
  )
}

export default CustomInput
